# 论文正文草稿（v0，2026-09-07）

> 状态：首稿。所有数字直接取自 `docs/paper.md` 主张-证据表；
> 标注 `[待盲比]` 的段落在用户完成 study_crop.html / study_ab60.html 后回填。
> 英文成稿前先用中文把论证链写顺——结构和逻辑错误在中文里更容易看出来。

---

## Title (working)

**When There Is No Right Answer: Evaluation Failure and a Sampling-Scale Fix
for Low-Resolution Pixel-Art Textures**

## Abstract (draft)

低分辨率像素画纹理生成暴露了生成模型评测的一个盲区：当输出只有 16×16
个离散色格时，同一材质的独立作者逐格一致率仅比配对零假设高 1.2 个百分点
（0.098 vs 0.086，50k+ 作者对）——**不存在唯一正确答案**。我们证明这使
两条主流自动评测路线同时失效：(1) 逐像素指标度量的是边缘分布拟合而非质量
（我们自己的三轮架构迭代中验证准确率持续上升而输出纹丝不动）；(2) VLM 判官
把人工盲比中 87% 的效应压缩到 62%（p=0.14）并抹平分层结构，位置偏好高达
67%。在有效评测（人工盲比）之下，一个平凡的降采样基线以 83:17 击败专门
设计的掩码预测模型，且与真人手绘不可区分（43%，p=0.68）[待 60 对扩样坐实]。
真正的瓶颈不在模型容量而在**采样尺度**：SDXL 无论输出尺寸如何都渲染约 25
个结构单元，而真人惯例是每瓦片约 4.5 个（两个独立来源测得）。按检测到的
结构主周期裁剪即可修复，在四个独立 run（两判官 × 两种子）中裁剪胜率
78–90%（最高 p=3e-11），适用条件（周期检出且各向异性 ≥0.20）由数据导出并
经留出材质验证。我们预注册并**证伪**了纯奈奎斯特解释——若失真全部来自
采样极限，裁剪收益应随分辨率升高而消失，实测 64 像素档仍胜 79–90%——
从而把机制修正为**结构单元惯例失配**，采样越界只是 16 像素下的加重因素。
该机制随后获得**预注册的因果支持，且呈剂量-反应**：以渲染分辨率为杠杆
操控单元数（27.7→13.8→9.4，操作检验独立通过），裁剪胜率单调下降
88%→54%→17%（中点与随机无异 p=0.749，端点差 −71.7pp、p=6e-9，
三种弃样处理一致）。

## 1. Introduction

论证链（每步一段）：

1. 任务：纯色图 + 材质词 → 低分辨率（16/24/32）像素画纹理。看似小任务，
   但它把"参考不唯一"推到极端：离散调色板 + 极少格子，使一致率可精确计算。
2. 事实一（评测失效同源）：作者间逐格一致率几乎等于配对零假设（B1/B7）。
   推论：逐像素损失/指标测不到质量（3.2，A3 实例）；VLM 判官同样失效但
   方式不同——方向可用、幅度与分层不可用（B6）。
3. 事实二（基线强于方法）：在人工盲比下，降采样基线击败专门方法 83:17
   （A4，预注册），且给定合适高分源时与真人不可区分（B2）。
4. 事实三（真瓶颈是尺度惯例）：新材质无高分源时基线弱，不是模型不会画，
   而是 SDXL 的结构单元数（~25/瓦片）与真人惯例（~4.5/瓦片）失配五倍余
   （B5/B9/B10）。按主周期裁剪修复之，四 run 稳健（5.4）。
5. 方法论贡献的一般性（**B16 已在第二领域复现**）：同一协议在三套独立
   emoji 重绘上复测，前景格 + 掩码内洗牌零假设下差 +0.0130，
   与材质的 +0.0117 几乎相同——"参考不唯一时逐格几乎无信号"是这类任务的
   共性，不是像素画特例。附带方法学警示（写进方法节）：不排除天然一致
   区域（背景）或用全图洗牌零假设，差值会虚高约一个数量级。

贡献列表（四条）：

- **诊断**：可精确计算的作者间一致率检验，证明本任务无唯一参考（3.1）；
  两条主流自动评测路线在此失效的实证（3.2/3.3）。
- **基线校准**：预注册人工盲比显示平凡基线胜过专门方法（4.2）、
  与真人不可区分（4.1）[待扩样]。
- **机制与修复**：结构单元惯例失配的测量（B9/B10）、按主周期裁剪的准则
  及其由留出验证的适用条件（5.2/5.5）[待盲比]。
- **评测方法论**：VLM 判官在本域的失效画像——67% 位置偏好、效应压缩、
  分层抹平、温度 0 下确定性导致"重跑复现"是假象（B6 + 判官确定性）。

## 2. Related Work

直接取 `related-work.md` 四节结构：

1. 像素画生成（SD-πXL、PixDiff-PIG）——指出其评测口径失效
2. 结构保持降采样（Kopf 2013、Öztireli 2015）——问题在其上游
3. VLM 判官（位置偏好文献 ~5% vs 本文 67%）
4. 高熵参考评测——本文给出可精确计算的检验

## 3. There Is No Right Answer

- 3.1 作者间一致率 0.098 vs 配对零假设 0.086（50753 作者对）。
  **诚实注**：早期用有偏零假设报告过"低于随机"，B7 撤回；写清配对构造。
- 3.2 推论 + A3 案例：acc 上升、画廊不动。
- 3.3 VLM 判官失效：87%→62%（p=0.14），分层 6%/33% → 37%/42%；
  位置偏好 67% 须正反两问；温度 0 下 35 对共同判定 100% 一致 →
  重跑不是复现，有效 n = 图对数而非调用数。
- 3.3 附（B15）：**换更强判官不解决**——gpt-5.6-sol 逐条一致率 53.8%（≈随机），
  结论层**倒转**而非压平（人 78% p=0.0012 → 它 42% p=0.41；先验层人 4% → 它 62%）。
  两厂商前沿模型同协议同图上以不同方式失败（gemini 压平、gpt 倒转），
  说明是任务级系统性问题，堵掉"换强模型即可"的反驳。claude-opus-5 验证在跑。

## 4. A Trivial Baseline Is Enough (When the Source Is Right)

- 4.1 基线 vs 真人：24 对 43%（p=0.68）；功效不足（65% 时 21%），
  已扩到 60 对 [待标注，合并分析须按材质去重]。
- 4.2 基线 vs 专门方法：72 对预注册，83:17；先验起作用处只赢 5%。

## 5. The Real Bottleneck: Structural-Unit Convention

- 5.1 失败诊断：五种降采样核在越界样本全败 → 不是核设计问题（B4/B5）。
- 5.2 修复：按主周期裁剪，目标 size/4.5 由 5979 张真人瓦片导出（5.3）。
  VLM 粗筛四 run：78%（gemini/s21）、88%（sonnet/s99）、
  五档口径 83%（gemini）/90%（sonnet，p=3e-11）[人工盲比待标，
  B6 表明 VLM 数字是低估]。
- 5.3 适用条件：周期检出 **且** 各向异性 ≥0.20；命中 70%→90%、
  误检 35%→6%；阈值在手挑材质上定、留出材质复验 93%/16%（5.5/5.6）。
- 5.4 预注册证伪与机制修正（论文最有区分度的一节）：
  - 预注册预测：纯采样解释 ⇒ 裁剪收益随分辨率递减。
  - 两个独立口径均证伪（ρ=−0.137 p=0.277；ρ=+0.044 p=0.64；
    48 档 100% ≥ 16 档 88%）。64 像素档裁剪仍胜 79–90%，
    而此时 47 像素周期已占 2.9 输出像素、高于奈奎斯特。
  - 修正后的机制：SDXL 画 ~25 个结构单元/瓦片，真人惯例 ~4.5
    （B5 瓦片与 B9 高分源两个独立来源一致）；蕴含裁剪因子
    25/4.5≈5.6，与实测 1/4.2–1/5 吻合。奈奎斯特越界只是 16 像素下的
    加重因素。**两种解释被失败的预测分开**：纯奈奎斯特预测梯度、被证伪；
    单元惯例预测无梯度、幸存。若梯度出现，两者将无法区分。
  - 本机制自带可证伪预测：让 SDXL 画更少单元应使裁剪收益缩小。
    **预注册操控实验证实（B12/B13）**：提示词杠杆全部失效（四种变体均 n.s.，
    "惯例深植"旁证），但渲染分辨率杠杆通过独立操作检验（384 使单元数
    32.5→9.3，−71.5%，MW p=0.0062；512 未过被正确拒绝）。假设检验：
    384 下裁剪胜率 **17% vs 对照 88%**（−71.7pp，p=6.3e-9，判据 −5pp），
    逐档一致，三种弃样处理下均成立（384 弃样率 60% vs 25% 的混杂如实报告）。
    补上 512 中点后成**剂量-反应**（B14）：单元数 27.7→13.8→9.4 对应
    88%→54%→17% 单调，512 恰在分水岭（p=0.749），弃样率 25→38→60% 同步上升；
    但**逐图**单元数不预测胜负（106 对 ρ=+0.098 p=0.316）——
    因果只在设置层面，可操作的是"1024 裁、384 不裁"的设置级规则。
    **机制从事后解释升级为有因果支持**：测量（B9）+ 因果（B13）+
    排除替代（B8/B10）。竞争解释"裁剪放大特征尺寸"同时被削弱——
    384 下裁剪同样放大特征却有害。
  - 诚实报告两条：(a) 门在 384 下仍触发 48% 而此时裁剪有害——门检测的是
    周期性结构而非尺度错配本身；(b) 逐图单元数**不**预测胜负（run 内方向
    不一致），因果效应只确立在条件层面，故不做"逐图门 v2"。均写进限制。
- 5.5 真人源为何不需要裁剪（B9）：125 个真人高分源降到 16 后周期占
  4.80 输出像素（仅 12% 越界），SDXL 渲染 0.73–0.95（越界五倍）。

## 6. End-to-End Demo

纯色 + 材质 → 纹理，`solid_color_task.png`（6 例，目视）。定位为演示而非主张。

## 7. Limitations & Honest Reporting

从 paper.md「风险与诚实说明」直接搬：单标注者；4.1 功效；窄领域；
方法简单（抗辩：由数据导出、修复被误诊的问题）；
5.10 竞争解释（裁剪同时放大特征尺寸）未排除。

---

## 图表清单（v0，按节排列；✅=已有可直接用的产物，🔧=需专门制图）

| 编号 | 内容 | 素材/脚本 | 状态 |
| --- | --- | --- | --- |
| Fig 1 | teaser：任务定义（纯色+材质词→三档纹理）+ 核心对比一眼可见 | `experiments/solid_color_task.png` + `final_showcase.png` 重排 | 🔧 重排版 |
| Fig 2 | 作者间一致率 vs 配对零假设（3.1 支点） | `figures/fig1_agreement.png`（`fig_agreement.py`） | ✅ 已渲染 |
| Fig 3 | A3 案例：acc 上升而画廊不动（3.2） | `experiments/metric/overview_fixed.png` + acc 曲线 | 🔧 需拼 acc 曲线 |
| Fig 4 | VLM 判官失效画像：效应压缩 + 分层抹平 + 位置偏好 67%（3.3） | `figures/fig4_judge.png`（`fig_judge.py`，B6 数字） | ✅ 已渲染 |
| Fig 5 | 基线 vs 专门方法样例网格（4.2 定性） | `experiments/compare/gallery_fixed.png` 选列 | 🔧 选例 |
| Fig 6 | 修复机制图：1024 渲染→检测主周期→裁剪→降采样 对比不裁 | `experiments/autocrop2.png` / `crop_scale.png` | ✅ 近似可用 |
| Fig 7 | 单元惯例失配 + 剂量-反应：三渲染档单元数分布（27.7/13.8/9.4）vs 真人 4.5，叠加胜率 88/54/17%（5.4，B10/B13/B14） | `figures/fig7_units.png`（`fig_units.py`，口径=period>0 按 prompt 去重，与 B14 精确吻合） | ✅ 已渲染 |
| Fig 8 | 梯度证伪：五档胜率 × 两口径，均无下降趋势（5.4） | `crop_res5.json` + `crop_res5b.json` | 🔧 纯数字作图 |
| Fig 9 | 剂量-反应主图：胜率 vs 源渲染单元数（17/54/88%，Jeffreys 区间，512 跨零效应线） | `figures/fig_dose_response.png`（`fig_dose.py`） | ✅ 已渲染 |
| Tab 1 | 人工盲比主表：4.1 / 4.2 /（5.2 待标） | paper.md 主张表 | 🔧 待盲比数字 |
| Tab 2 | 四 run VLM 粗筛汇总（判官×种子×档位） | 四个 JSON，数字已算好（见下） | ✅ 数字就绪 |
| Tab 3 | 适用条件门的命中/误检（定阈值批 vs 留出批） | 数字见下表；原始计数在远程第 1 轮输出，投稿前须补 n | ⚠ 百分比就绪，缺原始计数 |

制图优先级：Fig 2（已有）→ Fig 8 / Tab 2（纯数字、支撑最强主张）→
Fig 6 / Fig 1（定性版式）→ 其余。全部可离线制作，不依赖 GPU。

Fig 8 已渲染：`figures/fig8_gradient.png`（`analysis/paired/fig_gradient.py`）。

### Tab 2 数字（从 JSON 复核，2026-09-07）

| run（判官/种子/档位） | 触发 | 有效 | 弃(不一致) | API失败 | 总胜率 | p | 分档 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| gemini/s21 三档 run1 | 66 | 40 | 4 | 22 | 31/40=78% | 6.8e-4 | 16:13/14 24:8/10 32:10/16 |
| gemini/s21 三档 run2 | 66 | 42 | 3 | 21 | 33/42=79% | 2.7e-4 | 16:13/15 24:9/10 32:11/17 |
| sonnet/s99 三档 | 57 | 42 | 15 | 0 | 37/42=88% | 4.4e-7 | 16:14/16 24:12/13 32:11/13 |
| gemini/s21 五档 | 110 | 65 | 12 | 33 | 49/65=75% | 5.1e-5 | +48:8/12 64:11/14 |
| sonnet/s99 五档 | 95 | 62 | 32 | 1 | 56/62=90% | 3e-11 | +48:9/9 64:9/10 |

注：run1/run2 同种子同判官，仅测 API 可靠性，正文只算一个独立口径；
sonnet 三档触发 57 vs gemini 66 是因为种子不同导致渲染图不同、门触发不同。

### Tab 3 数字（双条件门：周期检出 且 各向异性 ≥0.20；来自第 1 轮）

**（2026-09-07 重做，各格计数已补齐）** 原表只留了百分比、原始计数遗失，
且"6+8 类手挑材质"清单没保存。现用 `analysis/paired/aniso_gate.py` 全部重算，
清单写死在脚本顶部，可本地复跑：

| 批次 | 门 | 命中率 | 误检率 | 命中−误检 |
| --- | --- | --- | --- | --- |
| 定阈值批（6 类方向性 240 张 / 8 类各向同性 312 张） | 仅周期 hi_frac=0.5（旧） | 193/240=80% | 43/312=14% | 67% |
| 定阈值批 | 仅周期 hi_frac=0.625 | 234/240=98% | 62/312=20% | 78% |
| 定阈值批 | **双条件门** | **231/240=96%** | **11/312=4%** | **93%** |
| **留出批**（词法规则划分，31+101 类 / 1234 张） | 仅周期 hi_frac=0.5 | 253/356=71% | 344/878=39% | 32% |
| 留出批 | 仅周期 hi_frac=0.625 | 341/356=96% | 462/878=53% | 43% |
| **留出批** | **双条件门** | **297/356=83%** | **157/878=18%** | **66%** |

注：阈值 0.20 早已固定，留出批是按材质名关键词划的（规则先定再看结果），
与定阈值批零重叠，两边关键词都沾的 11 类一律弃用。
留出批误检率偏高是因为词法划分把玻璃窗格、矿脉这类真有方向性的材质放进了
"各向同性"，故该数是上界。原表的 70%/35%→90%/6%、留出 93%/16% 无法复现，已弃用。

## 回填清单（标注完成后）

| 位置 | 现在写的 | 回填为 |
| --- | --- | --- |
| Abstract / 4.1 | 43% p=0.68 (n=24) | study_ab60 合并结果（去重后） |
| Abstract / 5.2 | VLM 粗筛数字 | study_crop 39 对人工盲比（需 27/39） |
