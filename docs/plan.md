# 推进计划（2026-08-24 起，约 3 天无人值守）

状态存在这个文件里并随 git push 同步。会话中断后，
新会话读这个文件即可从断点继续，不依赖 cron 存活。

## 为什么做这件事

2026-08-24 撤回了 S1 的证伪判定（见 `premise.md`）。原因：
现有七个特征（颜色数/平坦度/边缘锐度/主周期/连通块密度/连通块大小/游程）
对**肉眼明显的差异失明**——SDXL 源与真人源经同一条流水线产出的结果，
读数几乎一致，视觉上却一个是木板墙、一个只是竖条纹。

**尺子瞎，所有基于它的结论都不可信。** 在造出一把能用的尺子之前，
方法实现、方向选择、论文定位全部无从谈起。

**方向已由项目负责人于 2026-08-24 定下**：不换任务，就在原任务上推进，
补全"低分辨率下读得出材质"这条缺失的验收标准，然后据此做方法。
三个候选方向不再是待决事项，`README.md` 里那节保留作为记录。

需要澄清一点：造尺子**不是要换任务**。原任务（纯色图 + 材质 → 像素纹理）
本身没问题，问题是它的验收标准只写了区域/调色板/分辨率三条，
那条 11 秒的流水线全部满足却依然难看。
缺的那条约束是"低分辨率下读得出材质"，M1–M4 就是要让它**可测量**。
补全之后任务还是同一个任务。

## 步骤

- [x] **M1 造判别测试集**
      需要一批"人一眼能分好坏、而现有特征分不开"的成对样本，用来当尺子的靶子。
      做法：对 `data/contentdb/pairs.json` 里覆盖度高的材质（≥8 个高分辨率源），
      用同一条 `baselines/downsample_quantize` 尾巴，分别以
      (a) 真人高分辨率纹理 (b) SDXL 生成纹理 为源，产出 16/32/64 三档。
      产出：`experiments/metric/testset/`，以及一份 CSV 记录每对的
      材质、尺寸、两个源、七个旧特征的读数差。
      **验收**：至少 30 对，且旧特征在其上的分离 AUC < 0.65（确认旧尺子确实瞎）。

      **结论（2026-08-24）：测试集已建成并可用，但验收未通过——而这本身是重要发现。**

      代码 `analysis/metric/build_testset.py`，产出 45 对
      （15 种材质 × 16/32/64 三档），图 `experiments/metric/overview_fixed.png`。

      **第一版测试集是坏的，靠看图才发现。** 调色板从真人图提取却拿去量化
      SDXL 图，两者色彩分布不匹配，SDXL 的 wood/brick/dirt 像素全部落到
      同一个最近调色板条目上，**塌成纯色平板**。那是构造假象，不是 SDXL 的性质。
      修法：量化前做逐通道均值/标准差对齐。**数字当时是"通过"的（AUC 0.571），
      光看数字会把坏数据当成好结果。**

      修正后旧特征在 45 对上的分离度：

      | 旧特征 | 真人源中位 | SDXL源中位 | AUC |
      | --- | --- | --- | --- |
      | flat_frac | 0.177 | 0.105 | **0.753** |
      | run_mean | 1.381 | 1.217 | 0.695 |
      | comp_size_mean | 1.916 | 1.491 | 0.691 |
      | comp_density | 0.522 | 0.671 | 0.309 |
      | period_max | 13.000 | 8.000 | 0.671 |
      | edge_sharp | 18.000 | 24.000 | 0.363 |
      | n_colors | 15.000 | 15.000 | 0.504 |

      最强 0.753 > 0.65，**验收未通过**。

      **由此必须修正 2026-08-24 的"特征失明"判断。** 那个判断是从
      wood 一个例子推广出来的（同样犯了以偏概全，本项目第四次）。
      复验确认：单看 wood 那一对，加不加色彩对齐特征都接近；
      但在 45 对上 flat_frac 有 0.753 的分离度。
      **正确说法是：旧特征不瞎，只是不够利（0.753），不是零。**

      另一个看图才发现的事实：**并非真人源总是更好**。
      obsidian / mossycobble / ice / cobble / grass 这几种 SDXL 源明显更有结构。
      之前"真人源更好"同样是从 wood 一个例子得出的。

      对后续的影响：M2 的目标改为**超过 0.753**，而不是"从零造尺子"。

- [ ] **M2 试候选指标**
      在 M1 的测试集上逐个测，看哪个能把 (a)(b) 分开。候选按成本排序：
      1. 多尺度对比度：把图再降一半，看结构还剩多少（母题粗则存活）
      2. 自相关峰强度（不是峰位置）——板缝这类周期结构会给出强峰
      3. 频谱低频能量占比：粗母题集中在低频，细木纹在高频
      4. 连通块的**尺寸分布形状**（不是均值）：粗母题应有少数大块
      5. 感知嵌入：CLIP / DINO 对 16×16 放大图的特征距离
      **验收**：任一指标在 M1 测试集上 AUC > 0.80 即算找到尺子。
      基线是旧特征 flat_frac 的 **0.753**，必须明显超过它才有意义。
      若全部 < 0.80，如实记录并停下来问，不要硬凑。

- [ ] **M3 用新尺子重测原立论**
      拿 M2 找到的指标，重跑 S1 的实验（真人 16px vs 真人高分辨率降采样，
      392 材质）。这是 S1 做错的那件事，现在用不瞎的尺子重做。
      结论三选一并写清楚证据：立论成立 / 立论证伪 / 仍无法判定。

- [ ] **M4 量化"什么样的高分辨率源经得起降采样"**
      用新尺子测：真人源和 SDXL 源在降采样前的高分辨率图上，
      有什么可测的差别能预测降采样后的好坏。
      这直接指向生成端该优化什么。

- [ ] **M5 方法设计文档**
      综合 M2~M4，写 `docs/method.md`：要解决的问题一句话说清，
      候选方案至少三个并列出各自的代价和风险，给出推荐及理由。
      候选参考（不限于）：
      1. 生成端引导——让 SDXL 产出粗母题、高对比、低频主导的纹理
         （prompt 工程 / negative prompt / 低频引导 / LoRA）
      2. 直接在低分辨率空间优化，用 M2 的指标当目标函数
      3. 在真人像素画上微调，学"什么母题在小预算下还读得出"
      **不写代码**，这一步只产出文档。

- [ ] **M6 最小可行实验**
      挑 M5 推荐的方案，做**最便宜**的版本验证方向对不对。
      优先选不需要训练的（如生成端引导），用 M2 的指标量化是否改善。
      若最便宜的版本需要超过 15 分钟 GPU，**停下来写清楚需要多少资源再等人**。

## 边界（无人值守期间）

**只做**：本仓库内的分析、写文档、git commit / push、
以及**单次不超过 15 分钟**的 GPU 推理（SDXL 生成纹理属于此类，约 3 秒/张）。

**不做**：超过 15 分钟的 GPU 任务（如 SD-πXL 那种 200 分钟的 run）、
动 `RoundSquisheen` 以外的目录、任何往外部服务发送内容的操作、
**选定研究方向**（三个候选方向的取舍必须由项目负责人决定）。

遇到需要越界的情况：停下来，把情况写进本文件，等负责人回来。

## 纪律

- 结论如实。本项目已推翻过自己三次（16×16 崩溃、S1 证伪判定、任务已解决），
  这是正常的，继续保持。
- 每个定量结论要有数字。
- 每步做完：打勾、在下面写结论和数字、commit、push。
