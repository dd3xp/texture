# 自驱循环日志

用户 2026-09-06 授权：**自行推进「调研 → 换架构 → 不行再调研」的循环**，
不要在一处死磕，可以宽泛调研同领域不同类的工作。

## 任务（重新锚定）

**输入一张纯色图，输出在其上绘制低分辨率像素画风格纹理的图。**

- 分辨率档位：**16×16 / 24×24 / 32×32**（我定的三档，用户授权自选）
- 材质由文字指定（`brick` / `wood` / …）

## 循环协议

每一轮固定四步，每步都要留下可核对的产物：

1. **调研** — 记进 `docs/related-work.md`，写清"这条对我们有什么用/不能用"
2. **选型** — 在 `docs/loop.md` 本文件记下：选哪条、为什么、**预先写死的失败判据**
3. **实现 + 自测** — 小规模先跑通，产出画廊
4. **验收** — 先用 VLM 粗筛（B6：只能粗筛），再攒够一批交用户盲比

**失败判据必须在实现之前写死**，否则事后一定会替自己找理由。
本项目已因此栽过多次（A3 全阶段用无意义的准确率做判据）。

## 已排除的路线（不要重走）

| 路线 | 否掉它的证据 |
| --- | --- |
| 从零训练掩码预测模型 | A4：输给平凡基线 83:17 |
| 手工结构先验（周期/边框/错缝） | A4：先验起作用那层模型只赢 5% |
| 配对监督（基线→真人） | B1：作者间逐格一致率 0.098 < 随机 0.172，**不存在确定目标** |
| 逐像素指标做判据 | B1 同上 |
| VLM 判官做结论 | B6：把 87% 压成 62%（p=0.14），抹平分层 |
| 结构描述子尺子做质量代理 | A4：它的排序与人的偏好**反向** |

## 已确立的事实（可以站上去）

| 事实 | 出处 |
| --- | --- |
| 给定合适高分源时，降采样基线与真人手绘**不可区分** | B2（43%，p=0.68） |
| 但新材质没有这种源，SDXL 直出很弱 | B4（1024 无 LoRA 仅 1/4 可用） |
| 弱的原因是**超出采样定理**：SDXL 画 25 层砖塞进 16 像素 | B5 |
| 按主周期裁剪可修复，1/4 → 15/16（目视，待盲比） | B5 |
| 风格在单张瓦片里可恢复（42 类 44.4% vs 8.5%） | B3 |

## 轮次记录

（每轮追加：日期 / 调研要点 / 选型与失败判据 / 结果 / 去留）

### 第 0 轮 — 2026-09-06 起点

待办：B5 的盲比（`experiments/annotate/study_crop.html`，24 对）仍等用户标注。
不阻塞循环——先做调研与下一轮架构。
