# texture

像素画纹理合成 (Pixel Art Texture Synthesis)。

## 任务

输入一张平涂纯色图（例如纯棕色区域）+ 目标材质语义（例如 `wood`）+ 目标像素预算 `N`，
输出在该区域上绘制了该材质纹理的像素画图像。

> **⚠️ 这个任务已被一条四步基线解决，原研究假设已被证伪。方向待定，见"当前状态"。**

原核心研究假设：*低分辨率下的纹理不是高分辨率纹理的降采样，而是艺术家的重新设计。*

**状态：未被检验**（证伪判定已于 2026-08-24 撤回）。

曾用 392 种材质做不配对检验，七个特征全部无法区分真人 16×16 与
"高分辨率降采样再量化"，据此判定证伪。但随后发现**这七个特征对
肉眼明显的差异同样失明**——SDXL 源与真人源经同一条流水线产出的结果，
读数几乎一致（主周期完全相同、连通块密度差 0.008 以内），
视觉上却一个是木板墙、一个只是竖条纹。

尺子失效，所以"分不开"不构成"相同"的证据。详见 [docs/premise.md](docs/premise.md)。

## 硬约束

生成结果必须同时满足像素画的四条硬约束：

1. **固定小调色板**（通常 4~16 色）
2. **整数网格对齐**（cell 边界不能有非整数周期能量）
3. **零抗锯齿**（边缘是硬的）
4. **抖动承担明暗过渡**（dithering 是有结构的高频，不是噪声）

原本认为第 4 条是主要技术难点（"事后量化会摧毁抖动结构"）。
这一判断同样**未被有效检验**——依据的是那七个失明的特征。

## 开发流程

本机（无 GPU）负责开发、调试、小规模验证；需要 GPU 时同步到 `emnlp`（8x A100 80GB）运行。

```bash
# 同步到 GPU 机器
bash scripts/sync_to_emnlp.sh

# 远端工作目录
ssh emnlp
cd /mnt/data/kw/RoundSquisheen/texture
```

注意：`emnlp` 的 `/mnt/data` 剩余空间紧张（约 300G），大规模数据集下载前先确认。

## 目录

| 路径 | 用途 |
| --- | --- |
| `src/texture/` | 主代码包 |
| `scripts/` | 同步、启动、数据处理脚本 |
| `experiments/` | 实验配置与产出（不入库） |
| `data/` | 数据集（不入库） |
| `docs/` | 文献调研、设计文档 |
| `notebooks/` | 探索性分析 |

## 当前状态

阶段：**baseline 验证完成，研究方向待重新决定。**

### 已完成

| 工作 | 结论 |
| --- | --- |
| SD-πXL 复现（12 个 run） | 它**架构上没有区域约束能力**：轮廓 IoU 最好 0.86，把 ControlNet 从 0.15 调到 0.9 反而全线退化到 0.56。成本与输出分辨率无关，均约 200 分钟。详见 [docs/related-work.md](docs/related-work.md) |
| 核心假设检验（392 材质） | **已证伪**，七个特征全部分不开。详见 [docs/premise.md](docs/premise.md) |
| 简单基线 | `SDXL → 降采样 → 量化 → 硬裁`：四个分辨率轮廓 IoU 全 **1.0000**、调色板完全合规、端到端 **11 秒**（比 SD-πXL 每张输出快约 4400 倍） |

### 问题

本 README 开头陈述的任务**已被上表第三行那条四步流水线完整解决**。
原本三个立论里有两个已倒：

- ~~低分辨率纹理是重新设计~~ → 证伪
- SD-πXL 做不到区域约束 → 成立，但**一条三行的硬裁就能做到**，不构成方法贡献
- ~~任务本身有难度~~ → 11 秒解决

### 唯一还站着的线索

**低分辨率下的母题可读性。** 16×16 / 32×32 时，SDXL 源的流水线输出是竖向条纹，
没有清晰板缝；换成真人高分辨率纹理作源，同尺寸下板缝结构明确
（见 `experiments/dq_full/comparison.png` 第 2 列 vs 第 3 列）。

但存在一个**尚未解释的矛盾**：这个差距**视觉上看得见，统计上测不出**——
S1 试过的七个特征没有一个能分开。可能是特征选得不对，
也可能是视觉印象本身不可靠。

### 待决定

**任务本身没有问题，不需要换。** 卡点在于任务陈述缺了"要好看/读得出材质"
这条约束，而它目前无法测量——所以既没法优化，也没法向审稿人证明
比基线强。三个候选（**等项目负责人决定，未自行推进**）：

1. **补全验收标准（推荐）** —— **任务不变**，补上它缺的那一半：
   "低分辨率下读得出材质"。本 README 开头的任务陈述只约束了
   区域、调色板、分辨率，这三条那条 11 秒的流水线全都满足，
   所以它"完成"了任务却难看。缺的约束需要先能被**测量**，
   这正是 `docs/plan.md` 里 M1–M4 在做的事。
2. **换角度** —— 保留已有实验，做成 benchmark / 负面结果分析论文。
   注意：此路的底气原本是"证伪了立论 + 任务已被解决"，
   而这两条**都已撤回**，所以目前内容偏空。
3. **换方向** —— 不做这个题。

在方向确定前，不应继续投入方法实现。
