#!/bin/bash
# (M84) 重训复制臂 —— d=384、从零单阶段 44000 步、**只换训练种子**（seed 0 -> 1）
#
# ============================ 预注册判据（先于任何数据提交） ============================
# 本臂与已跑完的 scripts/m75_w384.sh（seed 0）构成一对「只差训练种子」的臂。
# 判据、作废条件、操作检验、预测全部写死在本文件头部；docs/arch_progress.md 同轮登记同一份文本。
#
# 【为什么现在开：这是上一轮判决自己点名的阻塞项】
#   (M83) 判 WIDTH_NULL_32 的同时登记了 (C2)③：本臂 rms(D)=0.5921，比 (M60) 的 0.4137
#   大 1.43 倍 ⇒ MDE 0.2027 ＝ (M58) 天花板 0.5127 的 40%。(M83) 第八节因此写死：
#   「再开宽度臂前必须先解决 rms(D) 更大这件事」。
#   ⚠ (M75)(M83) 与 (M60)(M62) 有一处**形状**差异：(M60)(M62) 两臂是**嵌套**的
#     （处理臂由控制臂 --init_from 续训而来），(M75) 两臂是**各自从零独立训练**的。
#   ⇒ 本臂问的就是这个差异：**两次同配置的独立从零训练之间，配对 CLIP@32px 上的 D 有多大？**
#   这是**构造零效应**臂（真值 D ≡ 0，两臂配置逐键相同、只差 --seed）⇒ 沿用 (M50)
#   `SHAM_BEATEN` 的招式：**造 sham 臂当经验零分布**，⛔ 不是借来的门槛。
#   train_trd.py:353 的原注释对此写得很直白：「换 seed = 换一次完整的独立训练
#   —— 这正是标定同配置重训漂移所需要的」。
#
# 【它回答的是什么、不回答什么】
#   ✔ 回答：「两臂都从零训」这种**实验形状**的 MDE 底在哪里 ⇒ 未来架构臂该用什么形状。
#   ⛔ 不回答：宽度有没有用（(M83) 判决原样不动，本臂一个字不改它）；
#   ⛔ 不回答：(M60) 的 +0.1250 是不是重训方差（那是**嵌套**臂，本臂量的是**从零**臂；
#      ⚠⚠ 引本轮结果时必须同引这一句，⛔ 不许把本轮的 rms 搬去重读 (M60)(M62)）。
#
# 【与 (M33) 的区别，必须披露】(M33) 量过「重训随机性不是主噪声源」，但那是 **KID 口径**，
#   账本已写死 ⛔ 不许搬到配对 CLIP。本臂是配对 CLIP@32px 上**第一次**量重训漂移。
#
# 【三层准入门】(M67) 的准入门原样**未满足**、用户**未**豁免（已挂起 11 轮）。
#   开臂依据与 (M75) 同：GOAL.md 防跑偏规则 4「结果不好就如实写不好，然后改架构再训，
#   不许转去做别的事来填满时间」+ 规则 3「进展必须能从 git log 看出来…附带训练日志」。
#   ⚠ 但本臂比 (M75) 多一条正当性：它是**上一轮判决文本自己点名的前置条件**（(M83) 第八节）。
#   ⇒ 本臂标定为**仪器标定臂**，不是架构臂；两条判决的唯一许可读法写死在下面。
#
# 【与控制臂的唯一差异】--seed 1（(M75) 控制臂是 --seed 0）与 --out。其余 52 个键逐键相同
#   （已按 remote_tmp/runs/trd_w384_09191345/config.json 的 54 键逐字对过）。
#   ⚑ codebook 由 build_codebook -> kmeans(cols, k) 构建，kmeans 的 seed 形参**写死默认 0**
#     （train_trd.py:51/69）⇒ codebook 与 --seed 无关 ⇒ 两臂 codebook_err 必须逐位相同
#     （= (OP5)，与 (M75) 同一条）。
#
# 【读数（另行在判读器里冻结，本文件只定判据）】
#   仪器＝配对 CLIP@32px：eval/diag_decompose.py --size 32 --xmodal --bs 8 --reps 2
#   --per_image --ckpt last.pt，K=28 个种子（与 (M60)(M62)(M75) 同口径）；
#   控制臂 28 份**直接复用** (M75) 的 remote_tmp/m75_w384_s0..27.json（同一条命令、同一个 run）。
#   统计单位＝**逐材质 n=67**（⛔ 不是 148 张）；配对符号翻转置换检验（20000 次，rng 12345），
#   import 冻结的 analysis/arch/m60_read_scale.py 与 analysis/arch/m75_read_width.py 本体。
#
# 【主判据】D = CLIP(seed 1) − CLIP(seed 0)，逐材质配对，
#   MDE_retrain = 2.8016 · rms(D) / sqrt(67)   （(M62) 实测口径，⛔ 禁用已被判错的 σ1 公式）
#   门槛冻结为 **0.1250** ＝ (M60) 那条"再训 12000 步"的增益，也是**全项目唯一量到过的真效应**。
#   (C1) MDE_retrain >= 0.1250  ⇒ **FROMSCRATCH_TOO_BLUNT**
#        唯一许可读法＝「『两臂各自从零训 44000 步』这种形状，连全项目最大的已知真效应
#        都测不到」⇒ 今后架构比较要么走**嵌套续训**（(M60) 形状），要么**每臂复制训练**。
#        ⛔ 不许读成"(M75) 的 null 不作数"（(M83) 判决原样成立，本臂不重读它）；
#        ⛔ 不许读成"重训方差很大所以模型不稳定"（只量了一个尺子上的一个统计量）。
#   (C2) MDE_retrain <  0.1250  ⇒ **FROMSCRATCH_USABLE**
#        唯一许可读法＝「从零双臂的重训底噪不足以掩盖 (M60) 量级的效应」。
#        ⛔ 不许读成"(M75) 的 null 因此更强"（那条臂自己的 MDE 0.2027 是实测值，本轮不改它）。
#
# 【判据②（只登记 + 一条警告，⛔ 不参与主判决）】
#   (D1) p_perm < 0.05 ⇒ 附加标签 **SHAM_FIRES**：构造零效应臂上出现了显著结果
#        ⇒ 整条配对 CLIP 管线在真值为零处假阳 ⇒ ⚠⚠ 今后引 (M60)(M62)(M75) 必须同引此标签。
#        ⛔ 反过来不成立：不触发**不**证明管线没问题（单次 5% 名义水平）。
#
# 【登记项（跑前写死，⛔ 不许判决后增删）】
#   rms(D_retrain)；与 (M75) 宽度臂 rms 0.5921 的比值；方差份额 rms^2 / 0.5921^2
#   （⚠ 加性分解假设「宽度效应与重训漂移独立可加」**未经检验**，⇒ 只登记、⛔ 不下判）；
#   mean_D、p_perm、逐材质正负计数、CI95、σ1 版 MDE（仅对账）。
#
# 【作废条件（任一触发则整轮 VOID，⛔ 不许改判据救）】按固定顺序，先触发者即判决：
#   (V3)/(OP1) 两臂读数份数 != 28，或每份长度 != 148，或材质数 != 67，或材质集合不同
#   (V4)       任一份读数出现 NaN
#   (V4)/(OP2) 任一份 real_half.CLIP != 34.1732177734375（逐位；(M61)(OP3) 参照行）
#   (V1)/(OP4a) 任一臂 log.json 末步 != 44000
#   (OP5)      两臂 codebook_err 不相同（数据侧不同源）—— ⚠ 故意排在 (V2) 之前，理由同 (M75)
#   (V2)/(OP4b) 两臂 config.json 除 {out, seed} 外有任何一个键不同
#   (OP3)      控制臂**内部**按种子奇偶对半的同一检验 p < 0.01（假阳）
#
# 【跑前方向预测（3 条，判决时逐条记分）】
#   (P1) 判 FROMSCRATCH_TOO_BLUNT（⚑ 押的是自己**不想要**的结果：它会关掉最省事的臂形状）。
#   (P2) SHAM_FIRES **不**触发（p_perm >= 0.05）。
#   (P3) 本臂 val 末值与控制臂 6.953 之差 < 0.0623（(M57) 同配方换种子的 val 噪声下限 Dmax）。
#
# 【本臂不做什么】⛔ 不碰 final_test.sh、⛔ 不开判官臂、⛔ 不动 (h)、⛔ 不改任何活件、
#   ⛔ 不改 eval/*、⛔ 不改 model/*、⛔ 不动 V_mat/E_mat、⛔ 零 API、
#   ⛔ 不回头重读 (M60)(M62)(M75)(M83) 任何一条判决。
# =======================================================================================
set -e
exec >> /tmp/m84_w384b.txt 2>&1
export CUDA_VISIBLE_DEVICES=7
export HF_HUB_OFFLINE=1
export TRITON_CACHE_DIR=/tmp/triton_m84_w384b
cd /mnt/data/kw/RoundSquisheen/texture
echo "=== m84_w384b start $(date -u) on gpu $CUDA_VISIBLE_DEVICES"
/mnt/data/kw/anaconda3/envs/jzs_train/bin/python -u model/train_trd.py \
  --out /tmp/runs/trd_w384b_09192215 \
  --seed 1 --reseed_after_build \
  --steps 44000 --lr 3e-4 --warmup 1000 --p32 0.5 --coarse --p_coarse 0.5 \
  --d 384 --heads 6 \
  --batch 256 --batch32 64 --wd 0.05 --drop 0.1 --depth 12 --codes 512 \
  --p_text_drop 0.1 --p_color_drop 0.5 --eval_every 1000 --sizes 16 32 \
  --bias_freqs 8 --bias_hidden 128 --level_emb --pal_aug 0.3 --pal_smooth 0.1 \
  --extra --extra_file train_extra_packs_only.json --n_ex 4 --p_ex_drop 0.3 \
  --n_domains 2 --save_at 22000 44000
echo "=== m84_w384b done $(date -u)"
echo "M84_W384B_DONE"
