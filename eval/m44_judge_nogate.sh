#!/usr/bin/env bash
# =====================================================================================
# (M44) 预注册：**把那道零 API pixfrac 筛子整个从"门"的位置上撤下来**，并用 (M43) 已经
# 生成好的那一对图，第一次让它在 `AMBIGUOUS` 带里接受判官检验。
# 本文件写于**任何判官读数存在之前**提交 = 预注册。⛔ 跑完不许改判据、不许改配方重跑。
#
# ⚠⚠ **`eval/m43_data_ab.sh` 一个字都不改** —— 那是 (M43) 的预注册，它的门（非 `SCREEN_GO`
#    就 `exit 3`）已经**按原样执行完毕**，(M43) 的结论就是"本轮不开判官"，那条记录不动。
#    本文件是**另一次独立的预注册**，只复用 (M43) 已落盘的图。
#
# ------------------------------------------------------------------- (1) 为什么现在撤这道门
# 三条理由，**全部与判官会判成什么无关**（此刻没有任何判官读数存在）：
#
# (a) **动态范围**：把同一把尺子、同一套配方量出的四个读数排在一条轴上 ——
#     D40 0.6431（`GEN_NULL`）、D38 0.6608（`CFG_NULL`）、**D_seed 0.6643＝只换种子，真效应
#     按构造为 0**、**(M43) 0.7907＝训练池 2.5 倍 + 来源条件 + 换码本，手上最大的杠杆**、
#     D37 0.8173（显著）。⇒ **纯噪声与最大的信号落在同一条 `AMBIGUOUS` 带里**。
#     而 GO 线 0.8173 是拿「**欠训的 step 3000 vs 训练完的 step 20000**」定的，那根本不是一次
#     杠杆比较。⇒ 这道门在真实杠杆所在的区间（0.66–0.82）**既拦不住噪声、也放不过最大的信号**。
# (b) **当门用就永远攒不到验证**（(M41) 自己写下的固有代价）：两次当门用（(M41) `NOGO`、
#     (M43) `AMBIGUOUS`）**两次都拦下、一次 GO 都没给过**，锚点至今仍是 n=3、零独立验证。
# (c) **去掉一道门不可能引入筛选偏倚**：门只决定"跑不跑"，不决定"报什么"
#     —— 本项目已有先例并已验证（`c5e67a9` / `557a50e`：65% 试点门整个去掉，
#     随后 `2c68bc4` 实证它误杀掉的正是全项目 1 号缺口的分母）。
#     前提照抄那次：**旧的筛子数字一次都不用来解读判官结果**，只登记。
#
# ⚠⚠ **诚实披露（不许假装不是）**：这个决定是在**看到 0.7907 之后**做的。
#    它之所以不是 p-hacking，靠的是两件可核验的事：①此刻**判官读数不存在**，
#    所以不可能是在挑一个想要的判决；②(M43) 的判据、读法、`decide()` 与预测 (P2)
#    **早在 `098e1e1`（第一张图存在之前）就盲写并提交了，本轮一个字不改**。
#    ⛔ 今后引用 (M43) 的判官结论，**必须同引"它的筛子读数是 AMBIGUOUS、门是看到该读数之后撤的"**。
#
# ------------------------------------------------------------------- (2) 判据：完全沿用 (M43) 预注册 (6)
# ⛔ 不新增、不修改任何一条。判读器 `analysis/arch/m43_read_data.py` 的 `decide()` 与
# `--selftest`（25/25）已在 `098e1e1` 提交，**本轮不动**：
#     族级 (U1) CI 偏 A 且不含 0.5 -> `DATA_V7_WINS`
#     族级 (U1) CI 偏 B 且不含 0.5 -> `DATA_V8_WINS`
#     含 0.5                        -> `DATA_NULL`
#     可解率对地板 21/118 不显著    -> `VOID_UNRESOLVABLE`
# ⚠⚠ 功效硬上限照旧：V_mat 分母 125、MDE ≈ **67.6%**；引任何 `_NULL` 必须同引。
# ⚠ 新臂跑完**当轮**登进 `analysis/arch/recheck_judge.EXPECT`（第 36 条 full）。
#
# ------------------------------------------------------------------- (3) 出图：不重新生成
# `/tmp/m43ab` 里的图是 (M43) 按逐字相同的 TRD16c 配方生成的，操作检验 8 项全过。
# ⚑ 生成管线**逐像素确定**（(M39)(M40) 三次实测 125/125 同图、指标逐位复现）
# ⇒ 重跑只会得到同一批图，**没有任何理由再烧一次 GPU**。本轮只花判官那一小时 API。
#
# ------------------------------------------------------------------- (4) 跑前预测（写死，跑完照录）
# (P2) **`DATA_V7_WINS`** —— 逐字沿用 `098e1e1` 里写的那一条（写于任何数据之前），⛔ 不改口。
# (P4) **对筛子的读法，两个方向都写死**：
#   - 判官**显著** ⇒ `AMBIGUOUS` 带里出了一个显著效应 ⇒ **GO 线 0.8173 被实证为过高**，
#     筛子**作为门正式退役**，三个锚点只留作历史读数。
#   - 判官 **`_NULL`** ⇒ 与筛子的判断一致（0.7907 < 0.8173），筛子得到**第 4 个数据点**；
#     ⛔ **但仍不恢复为门** —— 理由 (a)(b) 与判决无关，`_NULL` 也不能证明它有分辨力
#     （(M43) 的 `_NULL` 同样可能只是 MDE 67.6% 的功效不足）。
#   - ⛔ 两种情况**都不许挪 D37/D38/D40 的数值**，⛔ 不许为了让某个结果好看而换尺子。
#
# ------------------------------------------------------------------- (5) 本轮不许动
# ⛔ `eval/m43_data_ab.sh`（(M43) 的预注册，逐字节不动）、`analysis/arch/m43_read_data.py`、
#    `eval/final_test.sh`、`eval/judge_pairs.py`、`judge_cluster*.py`、`UNITS_PER_TILE`、
#    任何默认值、32px 准入条件①②③④、已下的任何判决。⛔ 本轮不训练、不重新生成。
#
# 用法（远程；凭据由 `tmux new-session -e ...` 注入，⛔ 不落盘）：
#   bash eval/m44_judge_nogate.sh
# =====================================================================================
set -u
P=${PY:-/mnt/data/kw/anaconda3/envs/jzs_train/bin/python}
REPO=${REPO:-/mnt/data/kw/RoundSquisheen/texture}
O=${OUT:-/tmp/m43ab}
J=${JOUT:-/tmp/judge_m43ab}
MET=${MET:-/tmp/m43_meter.json}
cd "$REPO"
export HF_HUB_OFFLINE=1
mkdir -p "$J"

# 操作检验那道门**照旧执行**（它与筛子是两回事：problems 非空 = 本臂作废）
NP=$("$P" -c "import json,sys;m=json.load(open(sys.argv[1],encoding='utf-8'));print(len(m['problems']) if 'problems' in m else 99)" "$MET") || exit 1
echo "[m44] (M43) 操作检验 problems 数 = $NP（99 = meter 里连这个键都没有，同样作废）"
[ "$NP" = 0 ] || { echo "[m44] 【禁】(OP1)-(OP5) 有 $NP 项不过 -> 本臂 VOID，不开判官、不花 API"; exit 4; }
S=$("$P" -c "import json,sys;print(json.load(open(sys.argv[1],encoding='utf-8')).get('screen'))" "$MET") || exit 1
echo "[m44] 筛子读数 = $S（按 (1) 本轮只登记、不当门；引用本臂结论必须同引这一行）"

[ -n "${VLM_BASE_URL:-}" ] && [ -n "${VLM_API_KEY:-}" ] || { echo "[m44] 缺 VLM 凭据"; exit 1; }
# (M40) 的教训：失效 key 会静默烧掉一小时 -> 先探针；账本：严格模式现命中两把 key，要取 tail -1 那把
curl -s -o /dev/null -w '[m44] key 探针 HTTP %{http_code}\n' \
  -H "Authorization: Bearer $VLM_API_KEY" "$VLM_BASE_URL/models"
while ps -eo args | grep -q '[j]udge_pairs.py'; do
  echo "[m44] 别的判官在跑，300s 后再看"; sleep 300
done
$P eval/judge_pairs.py full --no_gate --a data7_rr4 --b data8_rr4 \
   --set V_mat --size 16 --root "$O" --outdir "$J" || exit 1
echo M44_JUDGE_DONE
