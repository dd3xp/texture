# 项目目标（唯一、永久不变）

> **本文件只有用户本人可以修改。** 任何会话、任何无头轮次都不许改动、删减、
> "重新诠释"或"收窄"下面的目标。定于 2026-09-11，用户原话见文末。

## 目标

**设计并训练一个新的架构，专门做像素画纹理生成，在这个任务上达到 SOTA。**

### 任务定义

- **输入**：一张图，其中有一块纯色区域（例如纯棕色）+ 一个材质名（例如 `wood`）。
- **输出**：同一张图，那块区域被画上低分辨率像素画纹理，
  分辨率 **16 / 24 / 32**，配色跟随区域原本的颜色。

### 为什么需要新架构

现有做法都完成不了这个任务：

- **SD-piXL** 之类的像素化方法；
- **大模型生成 + 降采样**（包括本仓库目前的管线：SDXL 出 1024 图 → 按主周期裁剪 →
  降采样 → 量化。这是现成模型加启发式后处理，**不是我们的架构**）。

本仓库此前训练过的三个模型全部失败，**它们都不算完成目标**：
A4 掩码预测（人工盲比 17:83 输给朴素降采样）、LoRA 微调 SDXL（两次失败）、
ControlNet 约束生成（失败）。

### 怎样算完成

1. **搭基线**：至少包括 SD-piXL、大模型生成 + 降采样（本仓库现有管线）、
   朴素降采样、以及从网上调研到的其他相关方法（能跑的都跑）。
2. **用领域通用指标测**：先调研像素画 / 纹理生成领域论文实际在用的指标
   （如 FID/KID、CLIP 分数、可平铺性、多样性等），用它们对比新架构与所有基线。
3. **新架构在这些指标上全面领先基线** = SOTA。
4. 需要主观评审时，**可以用用户 API 里的大模型当评审**（凭据只从环境变量读）；
   但要按本仓库的去偏协议（正反两问、不一致弃用、先量可解率）。

## 不是目标的事（这些不许取代上面的目标）

- 写"评测为什么失灵"的论文、审计旧数字、复核旧实验、修论文措辞。
- 在 SDXL 输出上继续叠后处理启发式，然后花大量时间评测它们。
- 论文：**等新架构达到 SOTA 之后再写**，在那之前不动 `paper/`。

## 防跑偏规则（每一轮都要自查）

1. **每一轮开工先读本文件。** 开工前问自己：这一轮做的事，
   **能不能让新架构更接近在基线上领先？** 不能就不做。
2. 只有**挡住训练/评测的 bug** 才允许修；其他审计、复核、措辞修订一律不做。
3. **进展必须能从 `git log` 看出来**：大部分提交应落在 `model/`、`eval/`、`baselines/`，
   且附带训练日志或"新架构 vs 基线"的指标表。说不出"这一轮训了什么、指标对基线是多少"，
   就是跑偏了。
4. 结果不好就如实写不好，然后**改架构再训**，不许转去做别的事来填满时间。

---

用户原话（2026-09-11）：

> 我现在就一个明确的目标，这个任务，我需要用新的架构做好，因为现有工作比如 sdpixl
> 或者大模型降采样都没法完成，这个你可以搭个基线然后找一下网上有什么通用指标进行测试。
> 所以这才需要我这个新的架构，专门做纹理生成，并且要做到 sota。
> 把这个写到某个文件里，这个目标永远不变，如果需要评审，我的 api 里面有大模型，
> 设置好定时检测继续推进。
